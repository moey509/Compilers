# parser test [scoping in classes] 

a := true;
c := a;

interface Az<T>
{
	fun first() : Integer = a;
	fun second() : String = b;
}

class Bz<T>(a : Integer) extends Az<T>
{
	fun cAdd() : Integer<> = first() + 6;
}

if (a)
{
	c := false;
}

return ["blah"];
