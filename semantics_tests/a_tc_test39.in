# parser test [non-generic super call & calling things from super] 
interface Az<F>
{
	fun first() : Integer = 1;
	fun second() : String = "b";
}

class Bz<F>(a : Integer) extends Az<F>
{
	fun cAdd() : Integer<> = first() + 6;
}
#b := Bz<T>(1);
#a := b.cAdd();
return ["blah"];
